import { NgxDclTableSortingDirective } from './ngx-dcl-table-sorting.directive';

describe('NgxDclTableSortingDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxDclTableSortingDirective();
    expect(directive).toBeTruthy();
  });
});
